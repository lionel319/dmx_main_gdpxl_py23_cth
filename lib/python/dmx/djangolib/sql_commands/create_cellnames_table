CREATE TABLE cellnames (
    id INT NOT NULL AUTO_INCREMENT,
    project VARCHAR(250) NOT NULL,
    variant VARCHAR(250) NOT NULL,
    cellname VARCHAR(500) NOT NULL,
    thread VARCHAR(20) NOT NULL,
    PRIMARY KEY ( id )
);
